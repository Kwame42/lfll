CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
FILE(GLOB_RECURSE source_files *.c)
ADD_LIBRARY(lfll STATIC ${source_files})
TARGET_INCLUDE_DIRECTORIES (lfll PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
INSTALL(FILES liblfll.a DESTINATION lib)
INSTALL(FILES lfll.h DESTINATION include)
